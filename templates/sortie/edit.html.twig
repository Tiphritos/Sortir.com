{% extends 'base.html.twig' %}

{% block title %}Edit Sortie{% endblock %}

{% block body %}
    <h1>Edit Sortie</h1>

    {{ include('sortie/_form.html.twig') }}

    <a href="{{ path('app_accueil') }}">Retour à l'accueil</a>
    {% if sortie.etatsNoEtat.Id <=3 %}
{#    {{ include('sortie/_delete_form.html.twig') }}#}
        <form method="post" action="{{ path('app_sortie_delete', {'id': sortie.id}) }}" >
            {#    onsubmit="#}
            {#    return confirm('Etes vous sur de vouloir annuler cette sortie?');"#}
{#<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sortie.id) }}">#}

            <button class="btn">Annuler la sortie</button>

        </form>
    {% endif %}
{% endblock %}
