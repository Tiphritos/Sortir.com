{% extends 'base.html.twig' %}

{% block title %}CsvFile{% endblock %}

{% block body %}
    <h1>CsvFile</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ csv_file.id }}</td>
            </tr>
            <tr>
                <th>Participantfile</th>
                <td>{{ csv_file.participantfile }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_csv_file_index') }}">back to list</a>

    <a href="{{ path('app_csv_file_edit', {'id': csv_file.id}) }}">edit</a>

    {{ include('csv_file/_delete_form.html.twig') }}
{% endblock %}
